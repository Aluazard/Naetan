# Crown level (de jure) laws
# Laws ending with 0 will be default laws at game start
law_groups = {
	dw_first_night_law = {
		law_type = realm
		allowed_for_councillors = yes
	}
}

de_jure_laws = {

# Jus Primae Noctis

	# Not Active
	dw_first_night_law_0 = {
		show_as_title = yes
		group = dw_first_night_law
		default = yes
		potential = {
			holder_scope = {
				is_tribal = no
				is_nomadic = no
			}
		}
		allow = {
			always = yes
		}
		ai_will_do = {
			factor = 0
		}
		effect = {
			hidden_tooltip = {
				revoke_law = dw_first_night_law_0
				revoke_law = dw_first_night_law_1
			}
		}
	}
	
	# Active
	dw_first_night_law_1 = {
		show_as_title = yes
		global_revolt_risk = 0.02 # A 2% increase in the odds of a global revolt from such an unpopular law
		city_opinion = -5 # Very unpopular among the citizenry
		feudal_opinion = -5 # Very unpopular among the citizenry
		temple_opinion = -2 # The church tends to be a little more tolerant of a ruler's rights with the people.
		group = dw_first_night_law
		potential = {
			holder_scope = {
				is_tribal = no
				is_nomadic = no
			}
		}
		ai_will_do = {
			factor = 0
			modifier = {
				factor = 0.002 # Rare to do this
			}
			modifier = {
				factor = 0 # If the ai isn't lustful, then they won't do this
				OR = {
					holder_scope = {
						NOT = { trait = lustful }
					}
					NOT = { has_law = dw_first_night_law_0 }
				}
			}
		}
		effect = {
			hidden_tooltip = {
				revoke_law = dw_first_night_law_0
				revoke_law = dw_first_night_law_1
			}
		}
	}	
}
