#### These events are intended to provide a chain of events related to the act of sex, which can be referenced from multiple locations.
### Check the block for event_target requirements in order to not break localization, opinions, modifiers, pregnancies, etc.
## .100 - .199  Male and Female (consensual) - from the perspective of the male, on a bed

namespace = DWSex

### .100 - .199  Male and Female (consensual) - from the perspective of the MALE, on a bed
## event_target:male_lover_1  - If a futa female is the one 'driving' the experience, she can be set as the male_lover_1, although some images are clearly dudes.
## event_target:female_lover_1 
## supported flags for custom desc text are: dw_target_wife, dw_target_mom, dw_target_daughter

# .110 - Kissing/Fondeling

# .120 - Cunninglingus 

# .130 - BJ

# .140 - Missionary
narrative_event = {
	id = DWSex.140
	picture = sex_mf_missionary_1
	title = dw_love_making_title
	is_triggered_only = yes #
	desc = {
		trigger = { # It's my wife
			has_character_flag = dw_target_wife
		}
		text = DWSex140.0
	}
	desc = {
		trigger = { # It's my mother
			has_character_flag = dw_target_mom
		}
		text = DWSex140.1
	}
	desc = {
		trigger = { # It's my daughter
			has_character_flag = dw_target_daughter
		}
		text = DWSex140.2
	}
	desc = {
		trigger = { # generic
			NOR = {
				has_character_flag = dw_target_wife
				has_character_flag = dw_target_mom
				has_character_flag = dw_target_daughter
			}
		}
		text = DWSex140.3
	}
	option = { # Continue
		name = DWSex140A
		narrative_event = { id = DWSex.150 }
	}
	option = { # Cum inside
		name = DWSex140B
		event_target:female_lover_1 = {
			character_event = { id = DWCore.9000 }
		}
	}
	option = { # Pull out and finish
		name = DWSex140C
		narrative_event = { id = DWSex.190 }
	}
}

# .150 - Doggy
narrative_event = {
	id = DWSex.150
	picture = sex_mf_doggy_1
	title = dw_love_making_title
	is_triggered_only = yes #
	desc = {
		trigger = { # It's my wife
			has_character_flag = dw_target_wife
		}
		text = DWSex150.0
	}
	desc = {
		trigger = { # It's my mother
			has_character_flag = dw_target_mom
		}
		text = DWSex150.1
	}
	desc = {
		trigger = { # It's my daughter
			has_character_flag = dw_target_daughter
		}
		text = DWSex150.2
	}
	desc = {
		trigger = { # generic
			NOR = {
				has_character_flag = dw_target_wife
				has_character_flag = dw_target_mom
				has_character_flag = dw_target_daughter
			}
		}
		text = DWSex150.3
	}
	option = { # Anal
		name = DWSex150A
		narrative_event = { id = DWSex.181 }
	}
	option = { # Cum inside
		name = DWSex150B
		event_target:female_lover_1 = {
			character_event = { id = DWCore.9000 }
		}
	}
	option = { # Pull out and finish
		name = DWSex150C
		narrative_event = { id = DWSex.190 }
	}
}

# .180 - Ass appreciation

# .181 - Anal Sex
narrative_event = {
	id = DWSex.181
	picture = sex_mf_anal_1
	title = dw_love_making_title
	is_triggered_only = yes #
	desc = {
		trigger = { # It's my wife
			has_character_flag = dw_target_wife
		}
		text = DWSex181.0
	}
	desc = {
		trigger = { # It's my mother
			has_character_flag = dw_target_mom
		}
		text = DWSex181.1
	}
	desc = {
		trigger = { # It's my daughter
			has_character_flag = dw_target_daughter
		}
		text = DWSex181.2
	}
	desc = {
		trigger = { # generic
			NOR = {
				has_character_flag = dw_target_wife
				has_character_flag = dw_target_mom
				has_character_flag = dw_target_daughter
			}
		}
		text = DWSex181.3
	}
	option = { # Cum inside
		name = DWSex181A
		narrative_event = { id = DWSex.194 } # Anal creampie
	}
	option = { # Pull out and finish
		name = DWSex181B
		narrative_event = { id = DWSex.190 } # TODO replace with a cum on ass event when an relevant one is created
	}
}

# .190 - Male pulls out, cums on her stomach
narrative_event = {
	id = DWSex.190
	picture = sex_pullout_stomach_1
	title = dw_the_finish_title
	is_triggered_only = yes #
	desc = {
		trigger = { # It's my wife
			has_character_flag = dw_target_wife
		}
		text = DWSex190.0
	}
	desc = {
		trigger = { # It's my mother
			has_character_flag = dw_target_mom
		}
		text = DWSex190.1
	}
	desc = {
		trigger = { # It's my daughter
			has_character_flag = dw_target_daughter
		}
		text = DWSex190.2
	}
	desc = {
		trigger = { # generic
			NOR = {
				has_character_flag = dw_target_wife
				has_character_flag = dw_target_mom
				has_character_flag = dw_target_daughter
			}
		}
		text = DWSex190.3
	}
	option = { # Good times
		name = DWSex190A
	}
}
# .194 - Anal Creampie
narrative_event = {
	id = DWSex.194
	picture = sex_mf_anal_creampie_1
	title = dw_the_finish_title
	is_triggered_only = yes #
	desc = {
		trigger = { # It's my wife
			has_character_flag = dw_target_wife
		}
		text = DWSex194.0
	}
	desc = {
		trigger = { # It's my mother
			has_character_flag = dw_target_mom
		}
		text = DWSex194.1
	}
	desc = {
		trigger = { # It's my daughter
			has_character_flag = dw_target_daughter
		}
		text = DWSex194.2
	}
	desc = {
		trigger = { # generic
			NOR = {
				has_character_flag = dw_target_wife
				has_character_flag = dw_target_mom
				has_character_flag = dw_target_daughter
			}
		}
		text = DWSex194.3
	}
	option = { # Good times
		name = DWSex194A
	}
}

### .200 - .299  Female and male (consensual) - from the perspective of the FEMALE, on a bed
## event_target:male_lover_1
## event_target:female_lover_1 
## supported flags for custom desc text are: dw_target_husband, dw_target_dad, dw_target_son

### .300 - .399  Male and Futa (consensual) - from the perspective of the MALE, on a bed
## event_target:male_lover_1
## event_target:futa_lover_1
## supported flags for custom desc text are: dw_target_wife, dw_target_mom, dw_target_daughter

# .310 - Kissing/Fondeling

# .320 - Cunninglingus 

# .330 - BJ
narrative_event = {
	id = DWSex.330
	picture = sex_mfuta_bj_1
	title = dw_love_making_title
	is_triggered_only = yes #
	desc = {
		trigger = { # It's my wife
			has_character_flag = dw_target_wife
		}
		text = DWSex330.0
	}
	desc = {
		trigger = { # It's my mother
			has_character_flag = dw_target_mom
		}
		text = DWSex330.1
	}
	desc = {
		trigger = { # It's my daughter
			has_character_flag = dw_target_daughter
		}
		text = DWSex330.2
	}
	desc = {
		trigger = { # generic
			NOR = {
				has_character_flag = dw_target_wife
				has_character_flag = dw_target_mom
				has_character_flag = dw_target_daughter
			}
		}
		text = DWSex330.3
	}
	option = { # Cum inside mouth
		name = DWSex330B
		# TODO
		clear_target_flags = yes # Remove if we make an actual cum in mouth event
	}
	option = { # Pull out and finish
		name = DWSex330C
		# TODO
		clear_target_flags = yes # remove if we make an actual facial event
	}
}
# .340 - Missionary

# .350 - Doggy

# .380 - Ass appreciation

# .390 - Money shot

### .400 - .499  Female (non-Futa) and Futa (consensual) - from the perspective of the non-futa FEMALE, on a bed
## event_target:futa_lover_1
## event_target:female_lover_1 
## supported flags for custom desc text are: dw_target_wife, dw_target_mom, dw_target_daughter

# .460 - Cowgirl
narrative_event = {
	id = DWSex.460
	picture = sex_ffuta_cowgirl_1
	title = dw_love_making_title
	is_triggered_only = yes #
	desc = {
		trigger = { # It's my wife
			has_character_flag = dw_target_wife
		}
		text = DWSex460.0
	}
	desc = {
		trigger = { # It's my mother
			has_character_flag = dw_target_mom
		}
		text = DWSex460.1
	}
	desc = {
		trigger = { # It's my daughter
			has_character_flag = dw_target_daughter
		}
		text = DWSex460.2
	}
	desc = {
		trigger = { # generic
			NOR = {
				has_character_flag = dw_target_wife
				has_character_flag = dw_target_mom
				has_character_flag = dw_target_daughter
			}
		}
		text = DWSex460.3
	}
	option = { # Cum inside
		name = DWSex460B
		# TODO
		custom_tooltip = { text = dw_pregnancy_check_tooltip }
		hidden_tooltip = { event_target:futa_lover_1 = { character_event = { id = DWCore.9005 }}} # Pregnancy check for the female (player)
		clear_target_flags = yes # Remove if we make an actual cum in mouth event
	}
	option = { # Cum outside
		name = DWSex460C
		# TODO
		clear_target_flags = yes # remove if we make an actual facial event
	}
}

### .500 - .599  Futa and Male (consensual) - from the perspective of the FUTA, on a bed
## event_target:male_lover_1
## event_target:futa_lover_1
## supported flags for custom desc text are: dw_target_husband, dw_target_dad, dw_target_son

# .510 - Kissing/Fondeling

# .520 - Cunninglingus 

# .530 - BJ
narrative_event = {
	id = DWSex.530
	picture = sex_mfuta_bj_1
	title = dw_love_making_title
	is_triggered_only = yes #
	desc = {
		trigger = { # It's my husband
			has_character_flag = dw_target_husband
		}
		text = DWSex530.0
	}
	desc = {
		trigger = { # It's my father
			has_character_flag = dw_target_dad
		}
		text = DWSex530.1
	}
	desc = {
		trigger = { # It's my son
			has_character_flag = dw_target_son
		}
		text = DWSex530.2
	}
	desc = {
		trigger = { # generic
			NOR = {
				has_character_flag = dw_target_husband
				has_character_flag = dw_target_dad
				has_character_flag = dw_target_son
			}
		}
		text = DWSex530.3
	}
	option = { # Cum inside mouth
		name = DWSex530B
		# TODO
		clear_target_flags = yes # Remove if we make an actual cum in mouth event
	}
	option = { # Pull out and finish
		name = DWSex530C
		# TODO
		clear_target_flags = yes # remove if we make an actual facial event
	}
}
# .540 - Missionary

# .550 - Doggy

# .580 - Ass appreciation

# .590 - Money shot

### .600 - .699  Futa and Female (non-Futa) (consensual) - from the perspective of the FUTA, on a bed
## event_target:futa_lover_1
## event_target:female_lover_1 
## supported flags for custom desc text are: dw_target_wife, dw_target_mom, dw_target_daughter

# .660 - Cowgirl
narrative_event = {
	id = DWSex.660
	picture = sex_ffuta_cowgirl_1
	title = dw_love_making_title
	is_triggered_only = yes #
	desc = {
		trigger = { # It's my wife
			has_character_flag = dw_target_wife
		}
		text = DWSex660.0
	}
	desc = {
		trigger = { # It's my mother
			has_character_flag = dw_target_mom
		}
		text = DWSex660.1
	}
	desc = {
		trigger = { # It's my daughter
			has_character_flag = dw_target_daughter
		}
		text = DWSex660.2
	}
	desc = {
		trigger = { # generic
			NOR = {
				has_character_flag = dw_target_wife
				has_character_flag = dw_target_mom
				has_character_flag = dw_target_daughter
			}
		}
		text = DWSex660.3
	}
	option = { # Cum inside her pussy
		name = DWSex660B
		# TODO
		custom_tooltip = { text = dw_pregnancy_check_tooltip }
		hidden_tooltip = { event_target:female_lover_1 = { character_event = { id = DWCore.9000 }}} # Pregnancy check for the female (target)
		clear_target_flags = yes # Remove if we make an actual cum in mouth event
	}
	option = { # Pull out and finish
		name = DWSex660C
		# TODO
		clear_target_flags = yes # remove if we make an actual facial event
	}
}

### .700 - .799  Futa and Futa (consensual) - from the perspective of the FUTA
## event_target:futa_lover_1
## event_target:futa_lover_2
## supported flags for custom desc text are: dw_target_wife, dw_target_mom, dw_target_daughter

### .800 - .899  Trap and Futa (consensual) - from the perspective of the TRAP, on a bed
## event_target:trap_lover_1
## event_target:futa_lover_1
## supported flags for custom desc text are: dw_target_wife, dw_target_mom, dw_target_daughter

# .810 - Kissing/Fondeling

# .820 - Cunninglingus 

# .830 - BJ

# .840 - Missionary

# .850 - Doggy
narrative_event = {
	id = DWSex.850
	picture = sex_trapfuta_doggy_1
	title = dw_love_making_title
	is_triggered_only = yes #
	desc = {
		trigger = { # It's my wife
			has_character_flag = dw_target_wife
		}
		text = DWSex850.0
	}
	desc = {
		trigger = { # It's my mother
			has_character_flag = dw_target_mom
		}
		text = DWSex850.1
	}
	desc = {
		trigger = { # It's my daughter
			has_character_flag = dw_target_daughter
		}
		text = DWSex850.2
	}
	desc = {
		trigger = { # generic
			NOR = {
				has_character_flag = dw_target_wife
				has_character_flag = dw_target_mom
				has_character_flag = dw_target_daughter
			}
		}
		text = DWSex850.3
	}
	option = { # Cum in his ass
		name = DWSex850B
		# TODO
		clear_target_flags = yes # Remove if we make an actual cum in mouth event
	}
	option = { # Pull out and finish
		name = DWSex850C
		# TODO
		clear_target_flags = yes # remove if we make an actual facial event
	}
}

# .880 - Ass appreciation

# .890 - Money shot

### .900 - .999  Futa and Trap (consensual) - from the perspective of the FUTA, on a bed
## event_target:trap_lover_1
## event_target:futa_lover_1
## supported flags for custom desc text are: dw_target_husband, dw_target_dad, dw_target_son

# .910 - Kissing/Fondeling

# .920 - Cunninglingus 

# .930 - BJ

# .940 - Missionary

# .950 - Doggy
narrative_event = {
	id = DWSex.950
	picture = sex_trapfuta_doggy_1
	title = dw_love_making_title
	is_triggered_only = yes #
	desc = {
		trigger = { # It's my wife
			has_character_flag = dw_target_wife
		}
		text = DWSex950.0
	}
	desc = {
		trigger = { # It's my mother
			has_character_flag = dw_target_mom
		}
		text = DWSex950.1
	}
	desc = {
		trigger = { # It's my daughter
			has_character_flag = dw_target_daughter
		}
		text = DWSex950.2
	}
	desc = {
		trigger = { # generic
			NOR = {
				has_character_flag = dw_target_wife
				has_character_flag = dw_target_mom
				has_character_flag = dw_target_daughter
			}
		}
		text = DWSex950.3
	}
	option = { # Cum in his ass
		name = DWSex950B
		# TODO
		clear_target_flags = yes # Remove if we make an actual cum in mouth event
	}
	option = { # Pull out and finish
		name = DWSex950C
		# TODO
		clear_target_flags = yes # remove if we make an actual facial event
	}
}

# .880 - Ass appreciation

# .890 - Money shot

