targeted_decisions = {
    # Virgins remove their trait if it is no longer relevant.
	dw_remove_trait_virgin = {
		filter = self
		ai_target_filter = self
		potential = {
			character = FROM # targeting ourselves
			trait = virgin
            OR = {
                NOT = { is_virgin_trigger = yes } # No longer a virgin
                NOT = { has_dw_bodytrait_content = yes } # No longer using this game rule
            }
		}
		allow = {
            trait = virgin
		}
		effect = {
			remove_trait = virgin
		}
		ai_will_do = {
			factor = 1.0 #100% likely - assuming they meet the requirements
		}
	}
    # Self removal of dick and tit traits if the game rule is toggled off
    dw_remove_trait_dicktits = {
		filter = self
		ai_target_filter = self
		potential = {
			character = FROM # targeting ourselves
            has_dw_dicktit_trait = yes
            NOT = { has_dw_bodytrait_content = yes } # No longer using this game rule
		}
		allow = {
            has_dw_dicktit_trait = yes
		}
		effect = {
			remove_cock_trait = yes
            remove_tits_trait = yes
		}
		ai_will_do = {
			factor = 1.0 #100% likely - assuming they meet the requirements
		}
	}
}
