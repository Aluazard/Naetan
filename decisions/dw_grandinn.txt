decisions = {
    dw_visit_grand_inn = {
		ai_check_interval = 12 # Once a year
		potential = { has_valid_grand_inn = yes }
		allow = {
			has_valid_grand_inn = yes
			custom_tooltip = {
				text = dw_character_available_to_adventure
				dw_is_able_to_go_out_adventuring = yes
			}
			custom_tooltip = { # We're keeping visits to one a month for game balance
				text = dw_inn_visit_recovery_tooltip
				NOT = { has_character_flag = dw_inn_visit_recovery }
			}
		}
		effect = {
			character_event = { id = dw_grandinn.01 }
			set_character_flag = dw_inn_visit_recovery
			hidden_tooltip = { character_event = { id = dw_grandinn.9999 days = 30} }
		}
		ai_will_do = {
			factor = 0.5
			modifier = {
				factor = 0
					NOT = { ai_zeal = 40 } # With a zeal score of 40+, the person would view it as too sinful to attend
			}
		}
	}
}
